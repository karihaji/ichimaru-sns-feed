name: Update Instagram feed

on:
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: update-instagram
  cancel-in-progress: true

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Install browser runtime
        run: |
          npm ci
          npx playwright install --with-deps chromium
      - name: Fetch public Instagram posts
        run: npm run update:instagram
      - name: Validate data
        run: npm run validate
      - name: Commit changed data
        run: |
          if git diff --quiet -- public/data/instagram.json; then
            echo "No Instagram changes."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add public/data/instagram.json
          git commit -m "chore: update Instagram feed"
          git push
